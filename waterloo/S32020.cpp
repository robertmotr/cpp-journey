#include <iostream>
#include <algorithm>
#include <string>
#include <set>
#include <map>

using namespace std;

int main() {
    string ln;

    getline(cin, ln);

    string orig = ln;
    getline(cin, ln);

    int i = 0;
    while(i < ln.length()) {
        
    }
    return 0;
}